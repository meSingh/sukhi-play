/** Facts about the project that more than one page needs. */
export const REPO = 'https://github.com/meSingh/sukhi-play';
export const SITE = 'https://sukhiplay.com';
export const RELEASES = `${REPO}/releases/latest/download`;

/**
 * Google Analytics 4.
 *
 * Nothing is loaded until a visitor says yes, so this id sits unused on most
 * visits. It is public by nature -- it ends up in the page for anyone who
 * accepts -- so there is nothing to keep out of the repository.
 */
export const GA_ID = 'G-GXYHXFDLR8';
